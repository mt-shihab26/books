const undefined_type_value = undefined;
const null_type_value = null;

// They are themselves values, but
// they carry no information.

console.log(undefined_type_value);
console.log(null_type_value);
