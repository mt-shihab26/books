# অধ্যায় ২ – C ঝালাই

## Notes

-   Function:
    -   Input/Output
        -   `printf()`
        -   `scanf()`
    -   Math
        -   `sqrt()`
        -   `fabs()`
        -   `sin(x)`, `cos(x)`, tan(x)`
        -   `asin(x)`, `acos(x)` , `atan(x)`
        -   `atan2(y, x)`
        -   `pow(x, y)`
        -   `exp(x)`
        -   `log(x)`, `log10(x)`
        -   `floor(x)`, `ceil(x)`
    -   cstring
        -   `strlen()`
        -   `strcmp()`
        -   `strcat()`
        -   `strcpy()`
        -   `memset()`
-   Notes:
    -   `int`: `2^31`, `2*10^9` or `2e9`
    -   `Precision`
-   `a.cout <input.txt> output.txt`
-   bitmask
